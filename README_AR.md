# 🏥 MedRec - نظام التعرف الذكي على الأدوية

نظام متكامل للتعرف على الأدوية باستخدام الذكاء الاصطناعي مع قاعدة بيانات شاملة لمعلومات الأدوية.

## ✨ المميزات

### 🔐 نظام المستخدمين الكامل
- تسجيل وتسجيل دخول باستخدام JWT
- إدارة الملف الشخصي
- تغيير وإعادة تعيين كلمة المرور
- حذف الحساب

### 🏥 قاعدة بيانات الأدوية
- **كود فريد** لكل دواء (MED001, MED002, ...)
- **معلومات شاملة**: 
  - الاسم بالعربي والإنجليزي
  - الاسم العلمي والشركة المصنعة
  - الوصف والجرعة
  - الآثار الجانبية والتحذيرات
  - التصنيف والسعر
- **بحث وفلترة متقدمة**
- **API كامل** للوصول للبيانات

### 📸 التعرف الذكي على الأدوية
- رفع صورة الدواء
- تحليل تلقائي بالذكاء الاصطناعي
- ربط مع قاعدة البيانات
- نسبة ثقة (Confidence Score)
- سجل كامل للتحليلات

## 🚀 التثبيت والتشغيل

### المتطلبات
```bash
pip install -r requirements.txt
```

### الإعداد الأولي
```bash
cd medrec

# إنشاء قاعدة البيانات
python manage.py makemigrations
python manage.py migrate

# تحميل بيانات تجريبية (8 أدوية)
python manage.py load_sample_medicines

# إنشاء مستخدم إداري
python manage.py createsuperuser

# تشغيل السيرفر
python manage.py runserver
```

السيرفر: http://localhost:8000  
لوحة التحكم: http://localhost:8000/admin/

## 📁 الهيكل

```
medrec/
├── accounts/          # نظام المستخدمين والمصادقة
├── api/               # API الأدوية والتحليل
│   ├── models.py      # Medicine & ImageUpload models
│   ├── views.py       # API endpoints
│   └── management/    # أوامر إدارية مخصصة
├── core/              # خدمة الذكاء الاصطناعي
└── medrec/            # الإعدادات الرئيسية
```

## 🔌 API Endpoints

### المصادقة
- `POST /auth/register/` - تسجيل حساب جديد
- `POST /auth/login/` - تسجيل الدخول
- `POST /auth/logout/` - تسجيل الخروج
- `GET /auth/profile/` - عرض الملف الشخصي
- `PUT /auth/profile/update/` - تحديث البيانات

### الأدوية
- `GET /api/medicines/` - قائمة الأدوية مع بحث وفلترة
- `GET /api/medicines/{code}/` - تفاصيل دواء معين
- `GET /api/medicines/search/?q={query}` - بحث متقدم

### التحليل
- `POST /api/uploads/new/` - رفع صورة للتحليل
- `GET /api/uploads/` - سجل التحليلات

للتفاصيل الكاملة: راجع `API_DOCUMENTATION.md`

## 💊 الأدوية المتوفرة

تم إضافة 8 أدوية شائعة:
- بانادول (MED001) - مسكن
- كونجستال (MED002) - أدوية البرد
- فيفادول (MED003) - مسكن
- بروفين (MED004) - مضاد التهاب
- فيتامين سي (MED005) - فيتامينات
- أنتينال (MED006) - جهاز هضمي
- أوجمنتين (MED007) - مضاد حيوي
- فلاجيل (MED008) - مضاد حيوي

### إضافة المزيد من الأدوية

#### من خلال لوحة التحكم الإدارية:
1. افتح http://localhost:8000/admin/
2. اذهب إلى Medicines → Add Medicine
3. أدخل البيانات واحفظ

#### من خلال ملف CSV:
1. جهز ملف CSV (راجع `sample_medicines.csv` كمثال)
2. نفذ الأمر:
```bash
python manage.py import_medicines_csv path/to/your_medicines.csv
```

## 🧠 دمج موديل AI

الملف: `medrec/core/ai_service.py`

```python
def infer(image_path: str) -> dict:
    # استبدل هذا بموديلك الحقيقي
    
    # 1. حمّل الموديل
    model = load_your_trained_model()
    
    # 2. معالجة الصورة
    image = preprocess(image_path)
    
    # 3. التنبؤ
    prediction = model.predict(image)
    
    # 4. إرجاع النتيجة
    return {
        'medicine_code': 'MED001',  # الكود المكتشف
        'confidence': 0.85,         # 0.0 - 1.0
        'description': 'تم الكشف بنجاح',
        'alternatives': []          # اختياري
    }
```

## 📊 التقنيات المستخدمة

- **Backend**: Django 4.2+ & Django REST Framework
- **Authentication**: JWT (Simple JWT)
- **Database**: SQLite (قابل للتغيير لـ PostgreSQL/MySQL)
- **AI Integration**: مرن (TensorFlow, PyTorch, OpenCV, ...)
- **Image Handling**: Pillow
- **CORS**: مفعل للفرونت إند

## 📝 أوامر إدارية مخصصة

```bash
# تحميل بيانات الأدوية التجريبية
python manage.py load_sample_medicines

# استيراد أدوية من CSV
python manage.py import_medicines_csv medicines.csv
```

## 🔒 الأمان

- مصادقة JWT آمنة
- Token blacklisting عند تسجيل الخروج
- Validation كامل للبيانات المدخلة
- Protected endpoints
- CORS محدد (عدله في production)

## 📖 التوثيق

- `USAGE_GUIDE.md` - دليل الاستخدام الشامل
- `API_DOCUMENTATION.md` - توثيق كامل للـ API
- `sample_medicines.csv` - مثال لاستيراد الأدوية

## 🎯 الخطوات التالية

- [ ] تدريب موديل AI للتعرف على الأدوية
- [ ] إضافة المزيد من الأدوية في قاعدة البيانات
- [ ] تطوير واجهة مستخدم (React/Flutter)
- [ ] إضافة ميزات:
  - تاريخ انتهاء الصلاحية
  - تفاعلات الأدوية
  - نظام التنبيهات
  - تقارير وإحصائيات

## 📞 للاستفسار

راجع الملفات التوثيقية أو افتح issue في المشروع.

---

**تم التطوير بـ ❤️ لخدمة المجتمع الطبي**
