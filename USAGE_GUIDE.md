# نظام MedRec - التعرف على الأدوية

## 📋 نظرة عامة
نظام ذكي للتعرف على الأدوية من خلال الصور باستخدام الذكاء الاصطناعي. كل دواء له كود فريد ومعلومات تفصيلية.

---

## ✨ المميزات الجديدة

### 🏥 قاعدة بيانات الأدوية
- **كود فريد لكل دواء** (مثال: MED001, MED002)
- **معلومات شاملة**: الاسم بالعربي والإنجليزي، الاسم العلمي، الشركة المصنعة
- **تفاصيل الاستخدام**: الجرعة، الآثار الجانبية، التحذيرات
- **التصنيف**: مسكنات، مضادات حيوية، فيتامينات، إلخ
- **السعر والصور**

### 📸 التعرف الذكي على الأدوية
- **رفع صورة الدواء** ويتم تحليلها تلقائياً
- **نتائج دقيقة** مع نسبة الثقة (Confidence Score)
- **ربط تلقائي** بقاعدة بيانات الأدوية
- **سجل كامل** لكل عمليات الكشف

### 🔍 البحث والفلترة
- **بحث متقدم** بالكود أو الاسم العربي/الإنجليزي
- **فلترة حسب التصنيف**
- **ترتيب** حسب الاسم أو السعر أو التاريخ

---

## 🚀 التشغيل السريع

### 1. تثبيت المتطلبات
```bash
cd medrec
pip install -r ../requirements.txt
```

### 2. تجهيز قاعدة البيانات
```bash
python manage.py makemigrations
python manage.py migrate
```

### 3. تحميل بيانات الأدوية التجريبية
```bash
python manage.py load_sample_medicines
```
سيتم إضافة 8 أدوية شائعة (بانادول، كونجستال، بروفين، إلخ)

### 4. إنشاء مستخدم إداري
```bash
python manage.py createsuperuser
```

### 5. تشغيل السيرفر
```bash
python manage.py runserver
```
السيرفر يعمل على: http://localhost:8000

---

## 📱 استخدام الـ API

### تسجيل مستخدم جديد
```bash
POST http://localhost:8000/auth/register/
{
  "username": "user123",
  "email": "user@example.com",
  "password": "securepass",
  "first_name": "أحمد",
  "last_name": "محمد"
}
```

### تسجيل الدخول
```bash
POST http://localhost:8000/auth/login/
{
  "username": "user123",
  "password": "securepass"
}
```
ستحصل على `access` و `refresh` tokens

### عرض جميع الأدوية
```bash
GET http://localhost:8000/api/medicines/
Authorization: Bearer {access_token}
```

### البحث عن دواء
```bash
GET http://localhost:8000/api/medicines/search/?q=بانادول
Authorization: Bearer {access_token}
```

### عرض تفاصيل دواء بالكود
```bash
GET http://localhost:8000/api/medicines/MED001/
Authorization: Bearer {access_token}
```

### رفع صورة للتعرف على الدواء
```bash
POST http://localhost:8000/api/uploads/new/
Authorization: Bearer {access_token}
Content-Type: multipart/form-data

Body:
  image: [ملف الصورة]
```

النتيجة:
```json
{
  "id": 1,
  "image_url": "http://localhost:8000/media/uploads/2025/12/09/pill.jpg",
  "detected_medicine": 1,
  "medicine_details": {
    "code": "MED001",
    "name_ar": "بانادول",
    "name_en": "Panadol",
    "scientific_name": "Paracetamol 500mg",
    "dosage": "قرص واحد كل 4-6 ساعات",
    "price": "15.50"
  },
  "confidence": 0.85,
  "created_at": "2025-12-09T12:30:00Z"
}
```

### عرض سجل التحليلات السابقة
```bash
GET http://localhost:8000/api/uploads/
Authorization: Bearer {access_token}
```

---

## 🧠 دمج موديل الذكاء الاصطناعي

حالياً الكود يستخدم بيانات تجريبية. لدمج موديل حقيقي:

1. افتح ملف: `medrec/core/ai_service.py`
2. عدل دالة `infer()`:

```python
def infer(image_path: str) -> dict:
    # حمّل الموديل الخاص بك
    model = load_your_model()
    
    # معالجة الصورة
    image = preprocess_image(image_path)
    
    # التنبؤ
    prediction = model.predict(image)
    
    # إرجاع النتيجة بهذا الشكل:
    return {
        'medicine_code': 'MED001',  # الكود المكتشف
        'confidence': 0.85,         # نسبة الثقة (0.0 - 1.0)
        'description': 'تم الكشف بنجاح',
        'alternatives': [           # اختياري: بدائل محتملة
            {'medicine_code': 'MED002', 'confidence': 0.65}
        ]
    }
```

---

## 🔧 لوحة التحكم الإدارية

1. افتح: http://localhost:8000/admin/
2. سجل دخول بحساب الـ superuser
3. يمكنك:
   - ✅ إضافة/تعديل/حذف الأدوية
   - ✅ عرض جميع عمليات التحليل
   - ✅ إدارة المستخدمين
   - ✅ مراجعة نتائج الـ AI

---

## 📊 الأدوية المتوفرة حالياً

| الكود | الاسم | التصنيف | السعر |
|-------|-------|---------|-------|
| MED001 | بانادول | مسكنات | 15.50 ج.م |
| MED002 | كونجستال | أدوية البرد | 22.00 ج.م |
| MED003 | فيفادول | مسكنات | 18.00 ج.م |
| MED004 | بروفين | مضادات الالتهاب | 25.00 ج.م |
| MED005 | فيتامين سي 1000 | فيتامينات | 35.00 ج.م |
| MED006 | أنتينال | أدوية الجهاز الهضمي | 20.00 ج.م |
| MED007 | أوجمنتين | مضادات حيوية | 65.00 ج.م |
| MED008 | فلاجيل | مضادات حيوية | 28.00 ج.م |

---

## 📁 هيكل المشروع

```
medrec/
├── api/                          # تطبيق الأدوية والتحليل
│   ├── models.py                 # Medicine & ImageUpload
│   ├── serializers.py            # API Serializers
│   ├── views.py                  # API Views
│   ├── admin.py                  # لوحة التحكم
│   └── management/commands/      # أوامر مخصصة
│       └── load_sample_medicines.py
├── accounts/                     # نظام المستخدمين
│   ├── views.py                  # تسجيل/تسجيل دخول/ملف شخصي
│   └── serializers.py
├── core/                         # الخدمات الأساسية
│   └── ai_service.py            # خدمة الذكاء الاصطناعي
└── medrec/                       # الإعدادات الرئيسية
    ├── settings.py
    └── urls.py
```

---

## 🎯 الخطوات التالية

1. ✅ **تدريب موديل AI** للتعرف على الأدوية
2. ✅ **إضافة المزيد من الأدوية** في قاعدة البيانات
3. ✅ **تطوير واجهة المستخدم** (Frontend)
4. ✅ **إضافة ميزات إضافية**:
   - تاريخ انتهاء الصلاحية
   - تفاعلات الأدوية
   - بدائل الأدوية
   - إشعارات مواعيد الجرعات

---

## 📞 الدعم

للمزيد من التفاصيل، راجع ملف `API_DOCUMENTATION.md`
