# MedRec System Diagrams Documentation

هذا المجلد يحتوي على المخططات الفنية لنظام MedRec.

## 📊 المخططات المتوفرة

### 1. ERD (Entity Relationship Diagram)
**الملف:** `01_ERD.png`

مخطط علاقات الكيانات الأساسي يوضح:
- **كيانات النظام الثلاثة:**
  - **User**: المستخدمين
  - **Medicine**: الأدوية
  - **ImageUpload**: عمليات رفع الصور

- **العلاقات:**
  - User ←→ ImageUpload (1:N) - علاقة "uploads"
  - Medicine ←→ ImageUpload (1:N) - علاقة "detections"

### 2. EERD (Enhanced Entity Relationship Diagram)
**الملف:** `02_EERD.png`

مخطط علاقات الكيانات المحسّن يوضح:
- **تفاصيل الصفات (Attributes):**
  - الدوائر بلون **ذهبي** = Primary Key
  - الدوائر بلون **أخضر فاتح** = Unique Key
  - الدوائر **رمادية** = صفات عادية

- **العلاقات بشكل معياري:**
  - معينات (Diamonds) توضح نوع العلاقة
  - الأرقام 1 و N توضح Cardinality

### 3. System Architecture
**الملف:** `03_System_Architecture.png`

معمارية النظام الكاملة موزعة على طبقات:

#### 🖥️ Client Layer (طبقة العملاء)
- Mobile App (Flutter/React Native)
- Web App (React/Vue.js)
- API Tools (Postman)

#### 🌐 API Gateway
- NGINX (Load Balancer)
- CORS Middleware

#### ⚙️ Application Layer (Django REST Framework)
- **Auth API** (/auth/*) - نظام المصادقة
- **Medicine API** (/api/medicines/*) - إدارة الأدوية
- **Upload API** (/api/uploads/*) - رفع وتحليل الصور
- **Admin Panel** (/admin/*) - لوحة التحكم

#### 🔧 Service Layer
- **JWT Service** - إدارة التوكنات والمصادقة
- **AI Service** - التعرف على الأدوية من الصور
- **Serializers** - التحقق من صحة البيانات

#### 💾 Data Layer
- **Database** (SQLite/PostgreSQL) - قاعدة البيانات
- **Media Storage** - تخزين الصور

#### 🔌 External Services
- **AI Model** (TensorFlow/PyTorch) - موديل الذكاء الاصطناعي
- **Email Service** (SMTP) - إرسال البريد الإلكتروني

## 🎨 رموز المخططات

### في ERD:
- **مستطيلات ملونة** = كيانات (Entities)
- **أسهم ثنائية** = علاقات (Relationships)
- **1 و N** = نوع العلاقة (One-to-Many)

### في EERD:
- **مستطيلات** = كيانات
- **دوائر/بيضاوية** = صفات
- **معينات** = علاقات
- **خطوط** = ربط بين الصفات والكيانات

### في System Architecture:
- **خطوط متصلة (─)** = HTTP/REST API calls
- **خطوط متقطعة (- -)** = External API calls
- **ألوان مختلفة** = طبقات مختلفة في النظام

## 🔄 إعادة إنشاء المخططات

إذا أردت إعادة إنشاء المخططات أو تعديلها:

```bash
cd e:\Temp\GRAD
python generate_diagrams.py
```

سيتم إنشاء/تحديث المخططات في مجلد `diagrams/` بصيغة PNG بدقة 300 DPI.

## 📝 ملاحظات

- جميع المخططات بصيغة PNG بدقة عالية (300 DPI)
- مناسبة للطباعة والعرض التقديمي
- الألوان متناسقة ومنظمة حسب الوظيفة
- يمكن تعديل السكريبت `generate_diagrams.py` لتخصيص المخططات

---

**تم إنشاء المخططات باستخدام:** Python + Matplotlib  
**التاريخ:** ديسمبر 2025
